import { mockService } from './mockService';

/**
 * apiClient - Standalone mock client for A3 Cargo Digitizer & Operations Portal.
 * Operates 100% in-browser with localStorage persistence and dynamic client-side calculations.
 */
export const apiClient = mockService;
export default apiClient;
