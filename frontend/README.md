# A3 Cargo UI (Frontend)

Standalone React 19 + TypeScript + Vite + Tailwind CSS application with integrated client-side mock services and customs calculation engine.

## Quick Start

```bash
npm install
npm run dev
```

Build production bundle:
```bash
npm run build
```
